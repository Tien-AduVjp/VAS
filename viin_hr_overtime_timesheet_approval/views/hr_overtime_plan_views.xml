<odoo>
	<record id="view_hr_overtime_plan_form" model="ir.ui.view">
		<field name="name">hr.overtime.plan.form</field>
		<field name="model">hr.overtime.plan</field>
		<field name="inherit_id"
			ref="viin_hr_overtime_approval.view_hr_overtime_plan_form" />
		<field name="arch" type="xml">
			<xpath expr="//notebook" position="before">
				<field name="need_timesheets_approval" invisible="1" />
				<div class="alert alert-warning" role="alert"
					attrs="{'invisible':[('need_timesheets_approval','=',False)]}">
					There could be timesheet entries that match this overtime plan
					but have not been approved yet. Hence, they will not be taken into
					account during calculation of actual overtime. Please get them
					approved first.
					<button name="action_view_approval_await_timesheets" type="object"
						string="View approval await entries">
					</button>
				</div>
			</xpath>
		</field>
	</record>
</odoo>
