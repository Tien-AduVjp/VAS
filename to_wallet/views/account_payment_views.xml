<odoo>
	<record id="account_payment_form" model="ir.ui.view">
		<field name="name">account.payment.form.inherit.wallet</field>
		<field name="model">account.payment</field>
		<field name="inherit_id" ref="account.view_account_payment_form" />
		<field name="arch" type="xml">
			<div name="amount_div" position="after">
				<field name="wallet"
					   attrs="{'readonly': [('state', '!=', 'draft')],
					   'invisible': ['|', ('payment_type', 'not in', ('inbound', 'outbound')), ('partner_id', '=', False)]}"/>
				<field name="wallet_amount" attrs="{'invisible': ['|', '|', ('payment_type', '!=', 'inbound'), ('wallet', '=', False), ('partner_id', '=', False)]}" />
			</div>
		</field>
	</record>

	<record id="view_account_payment_search" model="ir.ui.view">
		<field name="name">account.payment.search.inherit.wallet</field>
		<field name="model">account.payment</field>
		<field name="inherit_id" ref="account.view_account_payment_search" />
		<field name="arch" type="xml">
			<filter name="inbound_filter" position="after">
				<filter name="wallet_filter" string="Wallet Payments" domain="[('wallet', '=', True)]" />
			</filter>
		</field>
	</record>
</odoo>
