<odoo>
	<record id="res_config_settings_view_form" model="ir.ui.view">
		<field name="name">res.config.settings.view.form.inherit.sale</field>
		<field name="model">res.config.settings</field>
		<field name="priority" eval="10" />
		<field name="inherit_id" ref="sale.res_config_settings_view_form" />
		<field name="arch" type="xml">
			<xpath expr="//div[@id='sale_config_online_confirmation_sign']/.." position="inside">
				<div class="col-xs-12 col-md-6 o_setting_box">
					<div class="o_setting_left_pane">
						<field name="prevent_unlink_related_invoices" />
					</div>
					<div class="o_setting_right_pane">
						<label for="prevent_unlink_related_invoices" />
						<div class="text-muted">
							Disallows the deletion on a sale order/sale order line if the order contains any related invoice
						</div>
					</div>
				</div>
			</xpath>
		</field>
	</record>
</odoo>
