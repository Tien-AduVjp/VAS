<?xml version="1.0" encoding="UTF-8"?>
<odoo>
	<record id="hr_overtime_rule_tree_view" model="ir.ui.view">
		<field name="name">hr.overtime.rule.tree</field>
		<field name="model">hr.overtime.rule</field>
		<field name="arch" type="xml">
			<tree string="Overtime Rules">
				<field name="name" />
				<field name="weekday" />
				<field name="hour_from" widget="float_time" />
				<field name="hour_to" widget="float_time" />
				<field name="holiday" />
				<field name="code_id" />
				<field name="pay_rate" />
				<field name="company_id" groups="base.group_multi_company" />
			</tree>
		</field>
	</record>

	<record id="hr_overtime_rule_form_view" model="ir.ui.view">
		<field name="name">hr.overtime.rule.form</field>
		<field name="model">hr.overtime.rule</field>
		<field name="arch" type="xml">
			<form string="Overtime Rule">
				<sheet>
					<group>
						<group>
							<field name="name" />
							<field name="weekday" />
							<label for="hour_from" string="Hours" />
							<div>
								<field name="hour_from" widget="float_time"
									class="oe_inline" />
								-
								<field name="hour_to" widget="float_time" class="oe_inline" />
							</div>
							<field name="holiday" />
						</group>
						<group>
							<field name="code_id" />
							<field name="code" />
							<field name="pay_rate" />
							<field name="company_id" groups="base.group_multi_company" />
						</group>
					</group>
				</sheet>
				<div class="oe_chatter">
					<field name="message_follower_ids"   />
					<field name="message_ids"  />
				</div>
			</form>
		</field>
	</record>

	<record id="action_hr_overtime_rules" model="ir.actions.act_window">
		<field name="name">Overtime Rules</field>
		<field name="res_model">hr.overtime.rule</field>
		<field name="view_mode">tree,form</field>
	</record>

	<menuitem id="hr_overtime_rule_menu"
		action="action_hr_overtime_rules" name="Overtime Rules"
		parent="hr_overtime_config_menu" sequence="10"
		groups="viin_hr_overtime.group_overtime_officer" />
</odoo>
