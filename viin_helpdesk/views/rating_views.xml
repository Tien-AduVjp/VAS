<odoo>
	<record id="rating_rating_view_search_team" model="ir.ui.view">
		<field name="name">rating.rating.search.team</field>
		<field name="model">rating.rating</field>
		<field name="inherit_id" ref="rating.rating_rating_view_search" />
		<field name="arch" type="xml">
			<xpath expr="/search" position="inside">
				<filter string="Last 30 Days" name="ftr_rating_last_30_days" domain="[
                    ('create_date', '>=', (datetime.datetime.combine(context_today() + relativedelta(days=-30), datetime.time(0,0,0)).to_utc()).strftime('%Y-%m-%d %H:%M:%S')),
                    ('create_date', '&lt;', (datetime.datetime.combine(context_today(), datetime.time(0,0,0)).to_utc()).strftime('%Y-%m-%d %H:%M:%S'))]" />
				<separator />
			</xpath>
		</field>
	</record>

	<record id="rating_rating_action_view_ticket" model="ir.actions.act_window">
		<field name="name">Ratings Ticket</field>
		<field name="res_model">rating.rating</field>
		<field name="view_mode">kanban,tree,graph,pivot,form</field>
		<field name="domain">[('consumed','=',True), ('res_model','=','helpdesk.ticket')]</field>
		<field name="search_view_id" ref="rating_rating_view_search_team" />
	</record>

	<record id="rating_rating_action_view_team" model="ir.actions.act_window">
		<field name="name">Ratings Team</field>
		<field name="res_model">rating.rating</field>
		<field name="view_mode">kanban,tree,form</field>
		<field name="domain">[('consumed','=',True), ('parent_res_model','=','helpdesk.team')]</field>
		<field name="search_view_id" ref="rating_rating_view_search_team" />
	</record>
</odoo>