<odoo>

	<record id="action_shorten_urls_in_name"
		model="ir.actions.server">
		<field name="name">Shorten URLs in Line's Description</field>
		<field name="model_id" ref="sale.model_sale_order" />
		<field name="binding_model_id" ref="sale.model_sale_order" />
		<field name="state">code</field>
		<field name="code">action = records.action_shorten_urls_in_name()</field>
	</record>

</odoo>
