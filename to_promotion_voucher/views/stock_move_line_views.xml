<?xml version="1.0" encoding="UTF-8"?>
<odoo>
	<data>
		<record id="view_move_line_tree" model="ir.ui.view">
			<field name="name">stock.move.line.tree</field>
			<field name="model">stock.move.line</field>
			<field name="arch" type="xml">
				<tree decoration-muted="state == 'cancel'"
					decoration-danger="(state not in ('cancel','done'))"
					string="Move Lines" create="0" default_order="date">
					<field name="date" />
					<field name="reference" string="Reference" />
					<field name="voucher_id" />
					<field name="product_id" />
					<field name="location_id" />
					<field name="location_dest_id" />
					<field name="qty_done" string="Quantity Done" />
					<field name="product_uom_id"
						options="{'no_open': True, 'no_create': True}"
						string="Unit of Measure" groups="uom.group_uom" />
					<field name="state" />
				</tree>
			</field>
		</record>

		<record id="view_move_line_search" model="ir.ui.view">
			<field name="name">stock.move.line.search</field>
			<field name="model">stock.move.line</field>
			<field name="arch" type="xml">
				<search string="Voucher Moves">
					<field name="reference" />
					<field name="voucher_id" />
					<field name="location_id"
						groups="stock.group_stock_multi_locations" />
					<field name="location_dest_id"
						groups="stock.group_stock_multi_locations" />

					<separator />
					<group expand="0" string="Group By">
						<filter string="Voucher" name="grp_voucher" domain="[]"
							context="{'group_by':'voucher_id'}" />
						<filter string="Status" name="grp_state" domain="[]"
							context="{'group_by':'state'}" />
					</group>
				</search>
			</field>
		</record>

		<record id="action_voucher_moves" model="ir.actions.act_window">
			<field name="name">Voucher Moves Analysis</field>
			<field name="res_model">stock.move.line</field>
			<field name="view_mode">tree,form</field>
			<field name="view_id" ref="view_move_line_tree" />
			<field name="search_view_id" ref="view_move_line_search" />
			<field name="domain">[('voucher_id','!=',False)]</field>
		</record>

		<menuitem name="Voucher Moves"
			parent="menu_voucher_report_root" sequence="1"
			id="menu_voucher_moves" action="action_voucher_moves"
			groups="group_promotion_voucher_manager" />

	</data>
</odoo>