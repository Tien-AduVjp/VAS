<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<record id="view_voucher_move_order_tree" model="ir.ui.view">
		<field name="name">voucher.move.order.tree</field>
		<field name="model">voucher.move.order</field>
		<field name="arch" type="xml">
			<tree string="Voucher Move Orders" decoration-info="state=='draft'" decoration-warning="state == 'confirmed'"
					decoration-success="state=='done'" decoration-danger="state=='cancel'">
				<field name="name" />
				<field name="location_id" />
				<field name="move_date" />
				<field name="state" />
			</tree>
		</field>
	</record>
	
	<record id="view_voucher_move_order_form" model="ir.ui.view">
            <field name="name">voucher.move.order.form</field>
            <field name="model">voucher.move.order</field>
            <field name="arch" type="xml">
                <form string="Voucher Move Orders">
	                <header>
	                	<button name="action_confirm" string="Confirm" type="object" states="draft" class="oe_highlight"/>
	                	<button name="action_move" string="Move" type="object" states="confirmed" class="oe_highlight" groups="to_promotion_voucher.group_promotion_voucher_manager"/>
	                	<button name="action_draft" type="object" states="cancel" string="Set to Draft" class="oe_highlight"/>
	                	<button name="action_cancel" type="object" states="confirmed" string="Cancel" class="oe_highlight" />
	                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,done"/>
	                </header>
	                <sheet>
	                    <div class="oe_title">
	                        <h1><field name="name" nolabel="1"/></h1>
	                    </div>
	                    <group>
	                        <group>
	                            <field name="origin"/>
	                            <field name="move_date" attrs="{'invisible': [('state', '!=', 'done')]}"/>
	                            <field name="user_id"/>
	                        </group>
	                        <group>
	                        	<field name="warehouse_id"/>
	                            <field name="location_id" invisible="1"/>
	                            <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
	                            <field name="picking_id" attrs="{'invisible': [('state', '!=', 'done')]}"/>
	                        </group>
	                    </group>
	                    <notebook>
	                    	<page string="Vouchers">
	                    		<field name="voucher_ids" domain="[('state', '=', 'new')]">
	                    			<tree name="voucher_ids">
										<field name="name" />
										<field name="product_id"/>
										<field name="lot_id" />
										<field name="issue_date"/>
									</tree>
	                    		</field>
	                    	</page>
	                        <page string="Moves">
	                        	<field name="move_finished_ids"/>
	                        </page>
	                    </notebook>
	                </sheet>
	                <div class="oe_chatter">
	                    <field name="message_follower_ids" widget="mail_followers"/>
	                    <field name="activity_ids" widget="mail_activity" />
	                    <field name="message_ids" widget="mail_thread"/>
	                </div>
                </form>
            </field>
        </record>
	
	<record id="voucher_move_order_filter" model="ir.ui.view">
		<field name="name">voucher.move.order.filter</field>
		<field name="model">voucher.move.order</field>
		<field name="arch" type="xml">
			<search string="Voucher Move Orders">
				<field name="name" string="Move Order" filter_domain="['|',('name','ilike',self),('origin','ilike',self)]"/>
                <separator/>
                <filter string="Draft" name="draft" domain="[('state','=','draft')]"/>
                <filter string="Confirmed" name="confirmed" domain="[('state','=','confirmed')]"/>
                <filter string="Done" name="done" domain="[('state', '=', 'done')]"/>
                <separator/>
                <field name="location_id"/>
                <group expand="0" string="Group By...">
                    <filter string="Shop / Location" name="grp_location" domain="[]" context="{'group_by':'location_id'}"/>
                    <filter string="Status" name="grp_status" domain="[]" context="{'group_by':'state'}"/>
                </group>
			</search>
		</field>
	</record>

	<record id="voucher_move_order_action" model="ir.actions.act_window">
		<field name="name">Voucher Move Orders</field>
		<field name="type">ir.actions.act_window</field>
		<field name="res_model">voucher.move.order</field>
		<field name="view_mode">tree,form</field>
		<field name="search_view_id" ref="voucher_move_order_filter" />
		<field name="help" type="html">
        	<p class="oe_view_nocontent_create">
        		Click to start a new voucher move order.
       		</p>
     	</field>
	</record>
	
	<menuitem id="menu_move_voucher" action="voucher_move_order_action" name="Move Vouchers to Shops"
			parent="menu_operation_issue" sequence="10" />
	
</odoo>
